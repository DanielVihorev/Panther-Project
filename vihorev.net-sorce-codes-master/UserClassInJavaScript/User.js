class User
{
    /*
private:
	string _username;
	SOCKET _sock;
public:
	User(string username, SOCKET sock);
	~User();
	void send(string message);
	//getters
	string getUsername();
	SOCKET getSocket();
*/

constructor(username,sock){
    console.log(`The User with the name ${username} Connected and using socket named ${sock}`)
}


};
